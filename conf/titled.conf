# titled module configuration
# title_command: shell snippet whose stdout becomes the title bar text
# title_interval: seconds between updates
title_command=printf "HackFM | MEM: %s/%s" $(free -h | awk '/^Mem:/{print $3,$2}')
title_interval=5
