# edit.conf - File editor handlers for hackfm
# Format: extension editor_command
# Use %f as placeholder for the filepath; if omitted, filepath is appended at the end
# If the command is not found, falls back to default_editor from hackfm.conf,
# then to the internal editor.
#
# Examples:
# py    nano
# sh    vi
# html  nano
md code

